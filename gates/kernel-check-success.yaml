{
  "schema": "vera.gate.v1",
  "id": "kernel-check-success",
  "description": "The latest kernel build, test, and lint check succeeded",
  "expires": "2026-10-16",
  "mode": "enforce",
  "source": "checks",
  "kind": "check.run",
  "selector": {"field": "command", "equals": "make kernel-check"},
  "condition": {"field": "exit_code", "equals": 0}
}
