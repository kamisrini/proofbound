# 2026-09-18 — P6 Task 8 frozen package acceptance

## CLI pilot and full rerun

- Extracted a clean Git checkout at frozen implementation commit
  `e4c8e77407699f7e089d5c1a2b3ce58df5871fbf`; no kernel code changes after that commit were found.
- Ran the calibrated one-candidate `internal/cli` integration pilot against a fresh disposable
  PostgreSQL 16.4 instance. Controls were `neutral=survived invalid=invalid lethal=killed`; candidate
  1 was killed. Total elapsed time, including calibration and tool startup, was 15.22 seconds.
- Selected a cap of two concurrent partitions. Each partition used a separate fresh PostgreSQL
  instance. The frozen CLI sweep completed as ranges 1–117 and 118–234: 234 killed, 0 invalid,
  0 survived. Elapsed times were 707.11 and 554.96 seconds; both calibrations passed.

## Projections rerun

- The frozen projections tree has 404 candidates in the source partitions previously used by the
  Task 8 evidence: `intent_report.go` 1–58, `projections.go` 59–345, and `report.go` 346–404.
- The intent/report partition completed 58/58 killed, 0 invalid, 0 survived, using the related
  intent, obligation, requirement, deployment, derived-state, and calibration-lethal tests.
- Full tagged package-suite partition 59–202 completed 144/144 killed, 0 invalid, 0 survived in
  3,852.81 seconds. Full tagged partition 203–345 completed 143/143 killed, 0 invalid, 0 survived
  in 3,311.33 seconds. Each used its own disposable database.
- The final report partition, 346–404, completed 59/59 killed, 0 invalid, 0 survived in 701.70
  seconds on a third dedicated disposable database. The complete projections total is 404/404,
  0 invalid, 0 survived.
- A first source-filtered invocation failed calibration because its selector excluded the
  harness-created lethal test; it stopped before candidate 1 and is excluded. A first unbalanced
  full-suite invocation for 59–345 was interrupted at 26 candidates to balance the two lanes; it
  is also excluded. The first report attempt found the disposable cluster lacked the `proofbound`
  database and failed its neutral control before candidate 1; after provisioning the database, the
  retry calibrated successfully. These attempts are excluded. The balanced replacement partitions
  have independent successful calibrations.
- PostgreSQL logs showed long checkpointer file-sync pauses on temporary-schema churn. The active
  disposable clusters now use `checkpoint_timeout=30min` and `max_wal_size=8GB`; transaction and
  durability settings remain default. Future projection partition databases use the same settings.

## Acceptance gate work

- Added a Task 8 package acceptance checker and wired the census `acceptance:<package>` probe to
  require the exact frozen commit, the 16-package universe, exact tree objects, calibrated candidate
  counts, zero invalid/survivors, tracked evidence, and an ACCEPTABLE non-author verdict. Census
  rows remain open until their evidence fields cite both the result matrix and verdict. Added shell
  tests for count mismatches, malformed manifest rows, wrong-commit binding, author identity, bad
  self-digest, and census evidence linkage. The complete result matrix and current-code verdict have
  been drafted but are not yet committed.
- Existing package reruns at `e4c8e77` remain: core 58, checks 36, Git 34, gitcmd 86, GitHub 58,
  intent 93, records 52, specdir 47, reviews 114, sessions 62, gates 109, migration 33, store 133,
  and twin 31; each has zero invalid or surviving mutants.
- No current-code all-package verdict exists yet. C3 remains open; Task 9 has not started.
- Prior `make check` result remains: repository checks and Go build/tests passed; the installed
  Go-1.26-built golangci-lint panics on Go 1.27 source. Direct generated-index and invariant checks
  passed. The generated artifact-integrity summary was updated to count the new schema-bearing
  verdict; index and invariant artifacts required no changes.

## Preservation

- Preserve the two pre-existing untracked paths and hashes recorded in `notes/state.md`.
- Stop all task-local PostgreSQL servers and remove `/tmp` mutation checkouts, databases, and caches
  after the remaining calibrated partitions finish.
