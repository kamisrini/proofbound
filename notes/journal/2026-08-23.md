# 2026-08-23 — Store survivor triage wrap

- Continued the Go kernel/store scaffold from the current repository state.
- Added defensive store tests for nil and closed handles, default/path configuration, nil locking,
  append conflicts, transaction commit/rollback/stop behavior, and ledger-write guards.
- Rebuilt and calibrated the mutation harness. The last database-aware store sweep before these
  tests found 113 candidates: 55 killed, 0 invalid, and 58 survivors. The sweep has not yet been
  rerun after the latest tests, so store mutation acceptance remains open.
- PostgreSQL integration tests use a disposable Docker container and the tagged integration test
  path; no database container is part of the repository state.
- `make check` was green before wrap-up. Tomorrow's first task is to rerun the DB-aware store sweep,
  triage every survivor, and record the acceptance decision before starting the Git connector path.

LESSON[test-cannot-see-the-property]: The initial store mutation run omitted tagged integration
tests, so a green-looking sweep could not exercise database invariants. The harness now has an
explicit integration-tag path and database-aware rerun instructions.

## EOD audit

- Rechecked the worktree, today’s history, state note, journal, and available repository artifacts.
- No additional source changes were made after the store triage wrap; the worktree was clean.
- `make check` passed. Its `index stale; run make index` line is the expected negative self-test,
  not a gate failure.
- No new recurring friction justified a code or process change tonight. The existing journal/state/
  check/commit/push/bundle protocol remains the smallest adequate control.
