# 2026-08-24 — Store mutation acceptance

## Store acceptance

- Ran the database-aware store sweep after `dc94712` against a disposable PostgreSQL 16
  container with `MUTANT_TEST_TAGS=integration` and `DATABASE_URL`.
- The first sandboxed attempt failed closed at the neutral calibration control because loopback
  sockets were denied. The host-loopback rerun calibrated correctly.
- Baseline rerun: 113 candidates, 90 killed, 0 invalid, 23 survivors. This improved the previous
  pre-`dc94712` result (55 killed, 58 survivors) but was not acceptance.

**HARM:** A green store suite could still allow the ledger to lose exclusivity, accept unsafe path
relationships, ignore a lost lock, leak database resources, present live transaction wrappers as
closed (or panic on closed wrappers), and permit mutation of the append-only event ledger.

**ROUTES to that harm:**

1. Close could return without releasing a live `flock`; a replacement lock-path inode was not
   distinguished from a missing path; and a deep DataDir/RuntimeDir overlap could be accepted.
2. An explicitly correct derived LockPath could be rejected, while the informational lock record
   could be omitted.
3. `Store.usable` could ignore `ErrLockLost`, allowing the next operation to proceed after
   dispossession.
4. Failed URL parsing, ping, embedded startup, or migration could take inconsistent cleanup paths
   and strand the lock/server.
5. Valid `Tx`, `Row`, and `Rows` wrappers could report `ErrClosed`; empty wrappers could panic;
   and `Rows.Close` could fail to close live rows.
6. Either `||` in the UPDATE/DELETE/TRUNCATE guard could become `&&`. The existing test did not
   detect this because its failure branch returned the unexpected `nil` error, falsely passing.
7. A redundant second path-overlap check made one predicate mutation equivalent and encouraged a
   fragile positional allowlist entry.

- Added route-level unit and integration tests, fixed the false-positive ledger-write assertion,
  made lock-record publication sequential and fail-closed, made failed-open cleanup uniform, and
  removed the redundant overlap check.
- Intermediate sweep: 109 candidates, 108 killed, 0 invalid, 1 equivalent survivor. The redundant
  route was removed rather than declared.
- **Acceptance sweep: 107 candidates, 107 killed, 0 invalid, 0 survivors.** Calibration was green.
  `docs/allowed-survivors.txt` therefore needs no store entry. Store mutation acceptance is closed.

## Mechanism gaps observed

- `make backup` is prescribed by `CLAUDE.md`, `notes/state.md`, `docs/gates.md`, and
  `VD-local-only-backup-mjic4a`, but the Makefile has no `backup` target. A manual pre-work bundle
  was created at `/home/s/Backups/proofbound-2026-08-24-pre-store-mutation.bundle`; the missing
  target remains a live mechanism defect to resolve separately.
- `make short` currently runs only `hooks-test`; despite being named as the inner loop, it runs no
  Go tests. The store unit suite was run explicitly rather than treating `make short` as evidence.

LESSON[test-cannot-see-the-property]: A test for three forbidden SQL forms returned the unexpected
`nil` error on its failure path, so two real guard mutations survived while the test stayed green.
The corrected assertion manufactures a non-nil diagnostic whenever `ErrLedgerWrite` is absent.

## Task 4 — Git connector implementation (author-ready, not accepted)

- The imported migration corpus claimed Task 4 was in Round-3 remediation and named committed
  verdict/code paths. Repository inspection proved those artifacts never existed in this Git
  history: only the parent connector SPEC had been imported, and it was misplaced at the child
  `gitcmd/SPEC.md` path. No local object, reflog, branch, tag, or remote ref could recover code or
  verdicts. Task 4 therefore started honestly from the committed contract rather than pretending
  the historical verdict was evidence about this implementation.
- Restored the parent SPEC to `internal/connector/git/SPEC.md`, wrote the child gitcmd SPEC before
  code, implemented the pure connector and standard-library Git adapter, and added real-repository
  fixtures for amend, two-commit rebase, branch switch, detached/tag history, older commit dates,
  shallow/grafted/replaced history, hostile paths/content, ref exclusions, and corrupt refs.

**HARM:** A green Git connector could permanently append incomplete or contradictory commit facts,
silently omit real rewritten/older history, fabricate decision citations, ingest stash/notes as
product history, or report a broken repository as a successful empty sync. Because the ledger is
append-only, a wrong payload for one SHA cannot be repaired in place.

**ROUTES to that harm:**

1. Shallow boundaries, legacy `.git/info/grafts`, and replacement objects can change ancestry and
   make a boundary commit's whole tree appear to be its changed files.
2. A date watermark can skip rebased, fetched, or deliberately older-dated commits; a hand-built
   ref scope can omit tag-only, non-checked-out branch, or detached-HEAD history.
3. Bare `--all` can ingest stash, notes, or replacement refs; prefix exclusions can accidentally
   drop legitimate names such as `refs/stashed`.
4. Greedy and lazy citation regexes can each fabricate a different prefix. Citations now resolve
   only when an exact `docs/decisions/<id>.md` exists in the observed commit's own tree.
5. Delimiter-shaped commit text, quoted/hostile filenames, scalar-placeholder swaps, or trimming
   can misattribute fields while producing valid JSON.
6. Git can ignore a corrupt loose ref while `rev-list --all` exits successfully; explicit loose-ref
   validation is required to keep broken distinct from empty.
7. Incorrect append counts, swallowed append errors, or a cursor used as an input can make a partial
   or skipped sync look complete.

- The first real-repository suite exposed two live assumptions before mutation: this environment's
  Git lacks `git init -b`, and `rev-list --all` stayed successful with a corrupt loose ref. Fixtures
  were made version-compatible; product code now validates loose refs.
- First gitcmd sweep: 63 candidates, 52 killed, 0 invalid, 11 survivors. Route tests closed token
  boundaries, exact ref classes, decision filename shape, and graft-stat control flow.
- Final gitcmd sweep: 60 candidates, 60 killed, 0 invalid, 0 survivors.
- First combined parent+child sweep: 77 candidates, 75 killed, 0 invalid, 2 survivors. Direct tests
  closed every uninitialized-connector guard route.
- **Final combined sweep: 77 candidates, 77 killed, 0 invalid, 0 survivors.** Calibration passed;
  no allowlist entry was needed. The mechanical Task-4 DoD and `make check` are green author-side.
- **Not accepted:** Build Law 9 requires a non-author adversarial verdict committed on receipt.
  No verifier was invoked in this session, so Task 4 remains open at that gate.

LESSON[context-state-drift]: Imported state and generated locks described code, verdicts, and gates
that were not present in this repository history. State claims were treated as hypotheses and
checked against files, refs, reflogs, unreachable objects, and the remote before implementation.

## Task 4 — current Round 1 independent verdict

The frozen `3470e60` implementation received `NEEDS_WORK`; the verdict is committed verbatim at
`docs/verification/verdicts/task4-current-round1.md` before remediation.

**HARM:** The connector can append incomplete commit facts or a fabricated citation, and can report
a broken repository as a successful sync. It can also collapse two distinct Git path byte strings
to one JSON string. Because event identity binds to payload content in an append-only ledger, those
observations cannot be repaired in place.

**ROUTES to that harm:**

1. A loose ref, packed ref, or detached HEAD can contain a syntactically valid object id whose
   object is absent; `rev-parse` can validate the spelling while history listing omits the object.
2. Different non-UTF-8 path byte sequences both become Unicode replacement characters during the
   Go string to JSON conversion, destroying Git path identity.
3. Taking only `path.Base` admits `docs/decisions/archive/VD-*.md` as though it were the direct
   decision path named by the contract.
4. `diff-tree` without a defined merge mode emits no paths for merge commits, so a real first-parent
   tree change is recorded as an empty `FilesTouched` list.
5. `--reverse --date-order` preserves ancestry and therefore is not the strict committer-date sort
   the parent interface prose claimed; the prose overstated a non-correctness convenience.
6. A typed-nil pointer stored in the `Repo` interface passes the constructor's plain nil check and
   creates a connector that fails only when used.

Remediation refuses missing targets for loose refs, packed refs, and detached HEAD; refuses
non-UTF-8 path streams; requires decision files to be direct children of `docs/decisions`; defines
every non-root commit's file set as its first-parent tree delta; corrects the ordering prose; and
rejects typed-nil pointer repositories at construction. Each route has a discriminating test; the
three missing-object cases exercise both `Commits` and `Tips`.

- `make check` is green after the remediation.
- gitcmd mutation sweep: **70 candidates, 70 killed, 0 invalid, 0 survivors**.
- combined parent/child mutation sweep: **88 candidates, 88 killed, 0 invalid, 0 survivors**.
- Task 4 remains open until a non-author reviews the frozen remediation and returns an ACCEPTABLE
  verdict; author-side green is not acceptance.

## Task 4 — current Round 2 independent verdict

The frozen `132e187` remediation received `NEEDS_WORK`; the verdict is committed verbatim at
`docs/verification/verdicts/task4-current-round2.md` before further remediation.

**HARM:** The same commit SHA can produce different payloads under ordinary repository
configuration, legal Git scalar bytes can be irreversibly rewritten while the connector reports
success, and a broken annotated tag can be silently omitted from a successful cursor. Because the
ledger is append-only and event identity binds to payload content, the contradictory or incomplete
observation cannot be repaired in place.

**ROUTES to that harm:**

1. `git diff --name-only` inherits `diff.renames`; toggling local config changes whether a pure
   rename records one path or both paths for the same SHA.
2. Subject, author name/email, and committer name/email bytes bypass UTF-8 validation; JSON replaces
   invalid bytes with U+FFFD while returning success.
3. An annotated tag's immediate object can exist while its referent commit is absent; validation
   passes the tag object and `Tips` swallows the peel failure.
4. The committed invalid-path test calls only the parsing helper, so the real root and non-root
   adapter routes can stop enforcing refusal without that test failing.
5. First-parent merge semantics were folded into permanent G-INV-12 instead of receiving an appended
   invariant id, and multiple proving tests were placed in one machine-readable citation cell.
6. The P1 plan retains the false strict date-order statement, and the parent SPEC dependency list
   omits the newly imported `reflect` package.

Remediation forces rename detection off for every Git command, refuses invalid UTF-8 in every
scalar field before constructing a `Commit`, follows annotated-tag referents during validation,
and treats detached non-commit HEADs as broken. Real-adapter tests toggle `diff.renames`, exercise
invalid UTF-8 root/non-root paths and a raw invalid-subject commit object, and cover loose, packed,
detached, annotated-tag, and valid non-commit ref routes.

The merge, rename, scalar-encoding, and tag-referent claims were appended as G-INV-15 through
G-INV-18. `make invariants-lock` was restored with a blocking drift detector and self-test; the lock
diff is exactly four appended rows. A blocking table-shape lint now rejects the Round-2 multiple-test
cell defect. Full historical citation resolution remains explicit migration debt because this
checkout consolidated the cited test files and omitted many named historical tests.

- The invariant-lock generator's first self-test exposed an argument-handling bug before the gate
  was trusted; the test now fails on any diagnostic from a clean generation.
- First post-remedy gitcmd sweep: 78 candidates, 74 killed, 0 invalid, 4 survivors. The survivors
  were mutually masking/redundant HEAD/peel guards, not declared exceptions.
- The HEAD decision was collapsed into validation, a detached-blob route was added, and redundant
  empty-output handling was removed.
- Final gitcmd sweep: **74 candidates, 74 killed, 0 invalid, 0 survivors**.
- Final combined parent/child sweep: **92 candidates, 92 killed, 0 invalid, 0 survivors**.
- Race tests and bare `make check` are green author-side. Task 4 remains open pending a non-author
  verdict on the frozen Round-2 remediation.

## Task 4 — current Round 3 independent verdict

The frozen `ce69370` remediation received `NEEDS_WORK`; the verdict is committed verbatim at
`docs/verification/verdicts/task4-current-round3.md` before further remediation.

**HARM:** The adapter can successfully ingest a different repository than the requested root, or
produce different `FilesTouched` for one SHA under ordinary submodule configuration. It can also
accept a nonstandard detached HEAD object while claiming every non-commit detached HEAD is broken.
Wrong-source and contradictory payload events cannot be repaired in place in the append-only ledger.

**ROUTES to that harm:**

1. Inherited `GIT_DIR` and `GIT_WORK_TREE` override `-C <root>` discovery and redirect every command
   to another repository while construction and listing both report success.
2. Other inherited `GIT_*` repository selectors can alter common dirs, object lookup, namespaces,
   indexes, or discovery; fixing only the two reproduced names does not close the class.
3. `diff.ignoreSubmodules=all` suppresses a changed gitlink while `none` reports it, re-keying the
   same SHA. Both repository-local and user configuration reach Git commands.
4. Root and non-root tree-delta commands are separate routes and both must pin gitlink behavior.
5. `HEAD^{commit}` accepts an annotated-tag object that peels to a commit; immediate-object and
   peelable-to-commit semantics were conflated in the detached-HEAD claim.
6. Stripping repository-selection environment must not break normal or linked-worktree discovery.

Remediation removes every inherited `GIT_*` variable from child processes so `root` alone selects
the repository, while linked worktrees continue to resolve through their `.git` files. Rename and
gitlink semantics are pinned by command configuration, and both root/non-root diff routes explicitly
request `--ignore-submodules=none`. Detached HEAD validation now checks the immediate object's type
rather than accepting anything peelable to a commit.

The independent claims were appended as G-INV-19 through G-INV-21 and the regenerated lock diff is
exactly those three new rows. Tests cover hostile `GIT_DIR`/`GIT_WORK_TREE`, removal of every
case-insensitive `GIT_*` key, linked worktrees, repository/user submodule config, root/non-root
gitlinks, and detached annotated-tag objects.

- Final gitcmd sweep: **77 candidates, 77 killed, 0 invalid, 0 survivors**.
- Final combined parent/child sweep: **95 candidates, 95 killed, 0 invalid, 0 survivors**.
- Race tests and bare `make check` are green author-side. Task 4 remains open pending a non-author
  verdict on the frozen Round-3 remediation.

## Task 4 — accepted in current Round 4

The independent verifier returned `ACCEPTABLE` for frozen commit `d794ff7`; the verdict is committed
verbatim at `docs/verification/verdicts/task4-current-round4.md`. It independently closed the entire
Round-3 route set across hostile Git environment variables, linked worktrees, persisted alternates,
repository/HOME/XDG submodule configuration, root/non-root/nested gitlinks, and immediate detached
HEAD object types. Bare `make check`, race tests, real repository integrity, the historical merge,
and both restored invariant mechanisms were green. It reported no HIGH, MED, or LOW findings.

**Task 4 is accepted under Law 9.** The accepted author evidence remains gitcmd 77/77 killed and
combined parent/child 95/95 killed, with zero invalids and survivors; mutation limits remain exactly
the residual limits stated in the verdict.

## Task 5 — witness emitter and checks connector (author-ready, not accepted)

Wrote `internal/connector/checks/SPEC.md` before implementation, grounded in
VD-verification-asymmetry-2dyjnd and VD-verdicts-are-artifacts-rl0rab. The shell-only
`make check-witnessed` path now records strict `vera.witness.v1` evidence without invoking VERA,
the connector validates and maps spool files to content-bound `check.run` events, and
`vera sync checks` owns the real store sync transaction.

**HARM:** A run that failed, emitted incomplete evidence, or came from different bytes could be
recorded as a successful check observation—or replayed under the wrong identity—inside an
append-only ledger. That would turn the verification substrate itself into durable false evidence.

**ROUTES to that harm:**

1. The wrapper could lose `make check`'s status, hash only one output stream, invoke the product it
   gates, reuse an id, or write a filename that disagrees with the body.
2. Ordinary struct decoding could invent missing zero-valued `exit_code`, `duration_ms`, or
   `git_dirty` fields; duplicate, unknown, trailing, malformed, or incomplete evidence could pass
   as v1.
3. A moved file could relabel `run_id`, while changed bytes under one run id could collapse onto the
   old observation instead of producing a visible content revision.
4. A cursor or filename cache could become the seen-set, malformed evidence could be skipped, or
   ingestion could delete the only local spool copy.
5. Directories ending in `.json` could enter the evidence set, and partially initialized connector
   states could bypass the defensive guard.
6. CLI wiring could omit the sync journal, fail to finish it with the connector error, swallow one
   side of the sync/finish error pair, or report replay as a new append.
7. A unit-only CLI sweep cannot reach store wiring. A tagged sweep that runs database packages in
   parallel can cross-contaminate assertions; a deadline shorter than its baseline can label a
   timeout as a killed mutant.

- Fixture tests prove successful and failing emission, exact combined-output digest, absence of a
  VERA dependency, unique self-consistent ULIDs, and repository/tool observations.
- A disposable PostgreSQL 16 container proved the canonical CLI route: first sync appended one,
  second sync appended zero/existing one; malformed evidence returns an error after journaling.
- A real repository `make check-witnessed` run emitted a passing witness with the exact captured
  output digest. Canonical `go run ./cmd/vera sync checks` then reported `appended=1`; immediate
  replay reported `appended=0 existing=1`.
- The first checks sweep found 35 candidates, 32 killed, 0 invalid, and 3 survivors. Route tests for
  `.json` directories and each partially initialized state closed all three. Final checks sweep:
  **35 candidates, 35 killed, 0 invalid, 0 survivors**.
- The DB-aware CLI sweep first found 17 candidates, 13 killed, and 4 survivors. Valid-command and
  malformed-evidence routes closed them. The sweep also exposed two harness defects before final
  acceptance: tagged packages shared one DB concurrently, then the fixed 10-second deadline was
  shorter than the serial baseline. Tagged mutation runs now use `-p=1` and a tested 30-second
  deadline; `scripts/tests/mutants.test.sh` runs that self-test under `make hooks-test`.
- Final DB-aware CLI sweep: **17 candidates, 17 killed, 0 invalid, 0 survivors**. No Task 5 survivor
  ledger entries are needed.
- The final duplicate-key strictness route added one connector operator; the post-change acceptance
  rerun was **36 candidates, 36 killed, 0 invalid, 0 survivors**.
- `make invariants-lock` appended C-INV-1 through C-INV-14. The invariant-table mechanism was
  generalized and self-tested for prefixed namespaces before the new table was trusted.
- ROADMAP already had the required content-digested/signing-deferred and row-set-identical wording.
  `.claude/commands/vera-wrap.md` does not exist in this checkout, so the planned step-3 amendment
  cannot be made and is recorded as migration debt rather than fabricated.

Task 5 remains open until a non-author adversarial verdict on a frozen commit is committed verbatim
and says ACCEPTABLE.

LESSON[mechanism-needs-self-test]: The tagged mutation harness had two sequential false-kill routes:
database package concurrency and a timeout below its own tagged baseline. Each was caught by live
calibration; serial tagged arguments and the longer tagged deadline are now one self-tested class.

## Task 5 — current Round 1 independent verdict

The frozen `baf5c11` author implementation received `NEEDS_WORK`; the verdict was committed
verbatim at `docs/verification/verdicts/task5-current-round1.md` before remediation.

**HARM:** An incomplete, failed, or differently sourced gate can become durable evidence of a
successful check, with the wrong repository identity or a fabricated clean state. Because the
ledger is append-only and content-bound, the false observation cannot be repaired in place.

**ROUTES to that harm:**

1. Present JSON `null` values for `exit_code`, `duration_ms`, and `git_dirty` decode into successful
   zero/false values even though missing fields are refused.
2. The wrapper runs `make check` in its caller's directory while recording the script repository;
   inherited `GIT_*` selectors can independently redirect repository identity.
3. A failed dirty-state observation is indistinguishable from clean in the boolean schema and was
   silently emitted as `git_dirty:false`.
4. Git stored the script as mode 100644 while Make executed it directly; the Windows worktree
   masked the fresh-checkout failure.
5. Shell escaping covers quotes and backslashes but not JSON control characters in tool output, so
   a successful gate can leave an uningestible witness.
6. A typed-nil appender passes the interface nil check and reports success when the spool is empty.

Remediation must discriminate every nullable field, bind gate and repository commands to one root,
strip the complete `GIT_*` selector class, fail before running the gate when repository observation
is unavailable, exercise the real Make target with a non-executable script, encode every JSON
control character, and reject a typed-nil appender before inspecting spool contents.

Remediation implements and verifies those route classes:

- Every top-level present-null field is rejected before typed decoding; the hostile table covers
  all eleven fields, including the three zero/false scalars that reached false evidence.
- The wrapper runs `make check` from its derived root. A fixture launched from another directory
  sets ten hostile `GIT_*` selectors, and its fake Git fails if any selector survives.
- HEAD failure, malformed HEAD, and status failure are checked before `make`; each produces no
  witness and a marker proves the gate never ran. This preserves the v1 boolean schema without
  fabricating an “unknown means clean” value.
- The real Makefile target explicitly invokes Bash. Its test copies the real target, sets the script
  to mode 0644, and runs the target end to end.
- JSON escaping covers named and generic U+0000–U+001F control routes; strict parsing round-trips
  backspace, form feed, tab, carriage return, U+0001, and U+001F.
- Typed-nil appenders are rejected before both empty and absent spool listing.
- The new independent claims were appended as C-INV-15 through C-INV-19 and the invariant lock was
  regenerated in the same remediation.
- The fresh-checkout test initially made mutation calibration fail because the scratch kernel did
  not include the root Makefile. The harness now copies that artifact and its own self-test proves
  the copy rather than weakening the product test.
- Post-remediation connector sweep: **37 candidates, 37 killed, 0 invalid, 0 survivors**. DB-aware
  CLI sweep: **17 candidates, 17 killed, 0 invalid, 0 survivors**. Focused race tests and bare
  `make check` are green.
- A real post-remediation `make check-witnessed` run succeeded. Canonical ingestion over all three
  accumulated real witnesses reported two new/one existing; immediate replay reported
  `appended=0 existing=3`.

Task 5 remains open pending a Round 2 independent verdict on the frozen remediation.

## Task 5 — current Round 2 independent verdict

The frozen `b634982` remediation received `NEEDS_WORK`; the verdict was committed verbatim at
`docs/verification/verdicts/task5-current-round2.md` before further remediation. Five Round 1
findings closed at their stated routes, but two invariant classes remained open.

**HARM:** A gate can successfully inspect repository B while the witness names repository A, and
distinct or incomplete tool-output bytes can collapse into one durable content identity. Both
produce append-only evidence whose claimed source or observed bytes cannot be recovered later.

**ROUTES to that harm:**

1. The wrapper removes inherited `GIT_*` only for its direct HEAD/status commands; `make check`
   inherits the hostile selectors and any Git-using gate can therefore inspect another repository.
2. Bash command substitution deletes NUL before JSON escaping, so a tool version containing NUL is
   silently rewritten while the gate and wrapper report success.
3. Raw invalid UTF-8 can enter the JSON file; Go decoding replaces distinct invalid bytes with
   U+FFFD, allowing 0xfe and 0xff evidence under one run id to deduplicate as one content identity.
4. A fake Make command that neither inspects its environment nor invokes Git cannot prove that the
   gate process shares the wrapper's sanitized repository environment.

Remediation must pass the complete sanitized environment into the gate, prove it with a real
Git-using gate across two real repositories, choose an explicit fail-closed byte admission policy
before Bash variable capture, reject invalid UTF-8 before Go decoding, and exercise NUL/0xfe/0xff
through both emission and actual connector identity routes.

Round 2 remediation closes those invariant classes:

- The same dynamically built `env -u GIT_*` argument set now wraps both direct Git observations and
  `make check`. A new test creates two real repositories; repository A's real Make target invokes
  real Git under selectors aimed at B, and both gate output and witness identity prove A's SHA while
  B's is absent.
- Tool versions are captured to files before entering Bash variables. The first-line byte policy
  rejects NUL using `od` and invalid UTF-8 using `iconv`; an inadmissible observation exits before
  `make`, emits no witness, and a marker proves the gate never ran.
- `readWitness` applies `utf8.Valid` to raw evidence before canonicalization or JSON decoding.
  Actual connector tests pass both 0xfe and 0xff variants and prove neither appends, so decoder
  replacement cannot collapse them into one identity.
- C-INV-20 and C-INV-21 were appended and the lock regenerated. The single C-INV-21 proving test
  exercises both emission refusal and actual connector refusal.
- Mutation calibration initially caught an over-specific real-Git assertion because nested Make
  adds entering/leaving diagnostics. The test now asserts the invariant—A present, B absent—rather
  than presentation text.
- Post-remediation connector sweep: **37 candidates, 37 killed, 0 invalid, 0 survivors**. DB-aware
  CLI sweep: **17 candidates, 17 killed, 0 invalid, 0 survivors**. Focused race tests are green.
- Bare `make check` and a real post-remediation `make check-witnessed` run are green with the byte
  admission and gate-environment mechanisms active.

Task 5 remains open pending a Round 3 independent verdict on the frozen Round 2 remediation.

## Task 5 — current Round 3 independent verdict

The frozen `f709651` remediation received `NEEDS_WORK`; the verdict was committed verbatim at
`docs/verification/verdicts/task5-current-round3.md` before further remediation.

**HARM:** A successful gate can still be reported as successfully witnessed when post-gate capture,
hashing, serialization, or publication failed; interruption during pre-gate version capture can
also leak temporary evidence files.

**ROUTES to that harm:**

1. `cat`, hash command, JSON write, or atomic move failures were unchecked, and the saved gate exit
   status overwrote the publication failure—returning zero with no witness or an uningestible one.
2. Version temp files were created before the cleanup trap, so interruption during a blocked version
   command leaked both files.
3. A cleanup test that killed only the shell left a child process alive; the test mechanism itself
   had to signal the process group before it could prove the product cleanup route.

Round 3 remediation checks every post-gate publication operation and returns non-zero on any failure;
the gate status is returned only after a valid witness is atomically published. Cleanup traps are
installed before any temp file is made, and version capture registers files in the parent shell
instead of a command-substitution subshell. The publication test removes the spool during the gate
and makes hashing fail; the same proving test interrupts a blocked version command as a process
group and asserts no version temp remains.

C-INV-22 was appended and the invariant lock regenerated. Focused checks tests pass after the
process-group test fix; the initial hang was a test harness route, not a product verdict.

Post-remediation verification is green: connector sweep **37 candidates, 37 killed, 0 invalid,
0 survivors**; DB-aware CLI sweep **17 candidates, 17 killed, 0 invalid, 0 survivors**; full kernel
race tests, DB-backed CLI integration, bare `make check`, and a real `make check-witnessed` run all
pass. Publication fixtures cover spool removal during the gate and a failing hash command, and the
interrupted capture fixture leaves no version temp files.

## Task 5 — Round 4 verdict and remediation

The frozen `7022fe6` Round 4 verdict was `NEEDS_WORK`, committed verbatim at
`docs/verification/verdicts/task5-current-round4.md` before remediation.

**HARM:** A helper observation can fail while the wrapper publishes a successful-looking witness,
or interruption can leave a blocked helper and temporary evidence behind.

**ROUTES to that harm:** failed `od` silently skipped the NUL scan; failed `date` still allowed
publication with the gate's status; and direct-PID TERM left the blocked version helper alive.

Remediation now checks `od`, clock, and run-id generation explicitly, runs version helpers with a
tracked child PID killed by cleanup, and adds C-INV-23 with focused failure fixtures. The invariant
lock was regenerated. Round 5 review is required; Task 6 remains gated.

Round 5 returned `NEEDS_WORK` at `1e6a0bf`, committed verbatim before remediation. **HARM:**
successful-empty `od` or `date` output was accepted as real observation, allowing rewritten tool
evidence or invalid witnesses. The remediation rejects empty byte streams and validates non-empty
RFC3339 timestamps, adds C-INV-24, and requires Round 6 review.

## EOD audit

- Rechecked the worktree, history, state note, journal, and verification artifacts; the tree was
  clean at pushed commit `d2bb315` before this closeout update.
- Bare `make check` passed. Its `index stale; run make index` line is the expected negative
  self-test, not a gate failure.
- Task 5 Round 6 `NEEDS_WORK` was committed verbatim. No Task 6 work was started.
- Next session resumes with malformed helper validation: exact `od` byte count, entropy length, and
  semantic timestamp ordering/range.
- Created and verified the dated recovery bundle:
  `/home/s/Backups/proofbound-2026-08-24-eod.bundle`.

LESSON[state-summary-drift]: A closeout audit found historical Round 3/4 wording and an incomplete
verdict-file glob in the live resume note. The EOD checklist now corrects the exact round/commit
status before the next session; no new automation is justified.

# 2026-08-25 — Task 5 Round 6 remediation

- Pre-flight bare `make check` passed; a complete recovery bundle was verified at
  `/tmp/proofbound-pre-task5-round6.bundle`.
- Remediated the Round 6 HIGH/MED routes in `kernel/scripts/check-witness.sh`: `od` output now
  covers the complete source byte count with decimal range validation; ULID entropy requires
  exactly 16 bytes; timestamps receive calendar, ordering, and non-negative-duration checks.
- Added malformed/truncated `od`, short entropy, impossible timestamp, and backward timestamp
  fixtures. Appended C-INV-25 and regenerated `docs/invariants.lock`.
- Verification: focused checks tests, `go test -race ./...`, bare `make check`, real
  `make check-witnessed`, and checks mutation sweep all passed; mutation result was 37/37 killed,
  0 invalid, 0 survivors.
- Committed as `517470e` (`fix: reject malformed witness helper output`). Task 5 remains unaccepted
  until a non-author adversarial review returns ACCEPTABLE; Task 6 was not started.

# 2026-08-25 — Task 5 Round 7 acceptance

- A fresh non-author adversarial review of frozen remediation `517470e` returned `ACCEPTABLE` with
  no HIGH, MED, or LOW findings. It independently checked the prior harm/routes, malformed-helper
  fixtures, race tests, bare gate, invariant mechanisms, and strict connector routes.
- The verdict was committed verbatim at
  `docs/verification/verdicts/task5-current-round7.md` in commit `05c1456`.
- Task 5 is accepted under Law 9. Task 6 is now unblocked; no Task 6 work has started.

# 2026-08-25 — Task 6 projections and verify implementation

- Published Task 5 acceptance first; branch was clean and synchronized before Task 6 work.
- Wrote `kernel/internal/projections/SPEC.md` before implementation. The package now owns derived
  projection DDL, commit/check reducers, atomic sequence checkpoints, rebuild, canonical row-digest
  snapshots, and future empty session/review views. CLI wiring adds `sync git`, `sync checks`,
  `sync all`, `rebuild`, `verify`, and a blocking `make verify` target.
- Bare `make check`, kernel unit tests, and `git diff --check` pass. The real `make check-witnessed`
  path passes. `vera verify` could not run end-to-end here because no `DATABASE_URL` or Docker is
  available and embedded PostgreSQL port 55432 is occupied.
- The projections mutation sweep was calibrated but produced survivors because all DB-backed proving
  tests are integration-tagged and no database was available; it was stopped rather than misclassified.
  Task 6 is not accepted. A dated recovery bundle is at `/tmp/proofbound-pre-task6.bundle`.

## Task 6 Round 1 remediation

- The independent Round 1 verdict is committed verbatim at
  `docs/verification/verdicts/task6-current-round1.md` (`a404602`) with two HIGH and five MED/scope
  findings.
- Remediation closes the latest-witness identity route, makes deferred session/review events fail
  closed, rejects malformed trailing JSON and invalid commit/check payloads, canonicalizes nested
  JSON snapshot values, and adds unique versioned projection metadata.
- Verification after remediation: `go test ./...`, `go test -race ./...`, bare `make check`, and
  `git diff --check` pass. DB-backed integration, mutation acceptance, and `vera verify` remain
  unproven due to unavailable PostgreSQL infrastructure; Task 6 remains unaccepted.

## Task 6 Round 2 remediation

- The fresh Round 2 verdict is committed verbatim at
  `docs/verification/verdicts/task6-current-round2.md`. It closed the Round 1 implementation
  routes but found the P1 scope boundary and commit-payload validation still unresolved.
- The plan now explicitly assigns sessions to Task 7, `vera report week` to Task 8, and the
  review-verdict chain to the P1 close requirement; Task 6's `sync all` is explicitly limited to
  implemented Git/checks connectors. Commit projections now validate identities, emails, subjects,
  paths, and decision citation shapes while preserving the Git connector's intentional null empty
  arrays. The proving citation path was corrected.
- Unit tests and bare `make check` pass. DB-backed integration, mutation acceptance, and end-to-end
  `vera verify` remain unavailable; Task 6 remains unaccepted pending the next review.

## Task 6 Round 3 review

- Fresh non-author review of `ae956f7` found no new HIGH/MED code defects and confirmed the scope
  reconciliation, strict validation, latest-witness comparison, and P-INV-1 through P-INV-11 code.
- It returned `NEEDS_WORK` solely because PostgreSQL infrastructure is unavailable: no
  `DATABASE_URL`, Docker access, free embedded PostgreSQL port, DB-backed tests, end-to-end
  `make verify`, or mutation acceptance sweep. The verdict is committed verbatim at
  `docs/verification/verdicts/task6-current-round3.md` (`cd8e3d8`).
- Task 6 remains unaccepted and Task 7 is not started.
## Task 6 PostgreSQL follow-up (2026-08-25)

The local `postgres:16-alpine` image was found and started as disposable container
`proofbound-task6-postgres` on host port 55433. External PostgreSQL exposed a concurrent migration
race, fixed in `2106ef5` with a transaction-scoped advisory lock; strict projection fixtures were
also corrected. CLI and store integration passed against the container; projections passed with
embedded integration; bare `make check` and `go test -race ./...` passed. `make verify` reached the
git adapter but its commit diff scan exceeded a 30-second bounded run. Task 6 remains unaccepted
pending that diagnosis, a complete mutation run, and fresh non-author review.

## Task 6 verify remediation (2026-08-25)

Follow-up `b9848f2` bounds Git commands and disables external diff/textconv helpers. The rebuilt
end-to-end `make verify` passed against the disposable PostgreSQL container. It also exposed and
fixed PostgreSQL JSON-value canonicalization in projection snapshots. The integration mutation
sweep failed calibration because its neutral mutant was killed; mutation acceptance and fresh
non-author review remain pending.

Round 4 independent review of `b9848f2` returned NEEDS_WORK with no new implementation defect.
The verdict is committed verbatim at `docs/verification/verdicts/task6-current-round4.md`.
Blockers are mandatory mutation calibration, a complete zero-survivor mutation sweep, and
independent reproduction of `make verify`.
