# 2026-08-27 — P4 acceptance close

The P4 implementation is independently accepted by the committed non-author verdict at
`docs/verification/verdicts/p4-current-round1.md`, but the package acceptance bar is not yet
met. A real uncached twin mutation run produced 31 candidates: 12 killed, 0 invalid, and 19
survivors. The final external-backed rerun killed all 31 candidates with 0 invalids and 0
survivors.

Repair work made the mutation harness execute copied trees with `-count=1`, calibrate the
selected package rather than all packages, and run the spec-first test against the harness's
kernel-only scratch layout. Focused twin and CLI tests pass, and bare `make check` passes with
`0 issues`.

The `vera verify` command now has a fifteen-minute deadline and stage-labelled errors. A clean
successful run and survivor disposition are complete. The two phase-review questions are
answered: the planned isolated replay/calibration capability exists, and the hardest imagined
failure was addressed through the survivor sweep, verifier deadline/staging, and witness-schema
boundary.

After folding Git parent lookup into the existing commit metadata query and allowing the
projection to validate every strict `make <target>` witness, a fresh external-PostgreSQL run
completed all stages: initial sync, four idempotence syncs, apply/snapshot, rebuild/snapshot,
witness read, and ledger witness lookup. It exited successfully with no error output.

P4 is closed under Law 9: the independent ACCEPTABLE verdict is committed, the package mutation
sweep is green, and the clean verifier evidence is complete.
