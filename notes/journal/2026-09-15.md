# Journal — 2026-09-15

## P6 Task 8 — projections mutation completion

- Resumed from the 2026-09-14 checkpoint. The projections package had 404 mutation candidates
  and its integration sweep was operationally expensive because every test opened and reset one
  shared external PostgreSQL database.
- Added external-DB test isolation by creating a unique schema per test, setting the test store's
  `search_path`, cleaning the schema on test cleanup, and running independent tests in parallel.
  The embedded-PostgreSQL fallback remains serial. The full tagged suite passed in 8.9 seconds
  after the optimization, versus a 25.7-second baseline.
- The optimization was committed as `4240cd6`. A race-detector attempt could not build because
  the environment has no `gcc`; normal untagged and tagged suites passed.
- Complete calibrated sweeps found and closed several real fixture gaps: missing check/session/
  review proof, report session/review/chain writer failures, nil reachability, source/kind chain
  filtering, finding and requirement-review insert-error identity, deployment validation fields,
  and shared-slice contamination in intent-reference validation. These remediations were committed
  as `2e674ed`, `b3e9e1a`, `d44b540`, `172f4c4`, `21b8bb5`, `12f0d74`, `be3c5c2`, and `be58cd7`.
- Final frozen implementation commit: `be58cd7`; exact projections tree object:
  `af840395f0211de0fe375f3d2ef1599e97e4a638`. Final result: 404 candidates, 404 killed, 0 invalid,
  0 survived. The full tagged package suite passed. Evidence is committed in
  `docs/verification/p6-task8-projections-mutation-be58cd7.md`.

LESSON: Parallel integration mutation execution is safe only when each test owns an isolated
database namespace and package-global test hooks are excluded or synchronized. The acceptance bar
still requires complete calibrated candidate coverage and a separate full tagged package run;
partial or focused output is diagnostic until the complete partition finishes.

LESSON: Error-path tests must assert semantic error identity when an inverted guard can leave the
database transaction aborted; a generic non-nil assertion can pass for the wrong failure.

## Verification gate

- Regenerated and committed `docs/verification/p6-event-route-matrix.md` after the projections
  freeze (`22eaff4`). The bare Snap Go gate could not run because of the known missing capability;
  the host Go gate reached `kernel-check`, then the installed Go-1.26-built `golangci-lint` panicked
  on Go 1.27 source loading. No repository test failure was observed in that gate run.

## P6 Task 8 — store and twin completion

- Added focused store tests for embedded directory/identity/startup cleanup, migration failure
  routes, replay and historical import validation, rollback, and sequence-update failures. The
  mutation collector now respects active build constraints, so Linux acceptance does not count
  unreachable Windows-only source candidates; Windows coverage remains governed by the Windows
  contract.
- Store remediation was committed as `eb40978`; the isolated replay sequence SQL correction was
  committed as `e4c8e77`. The final frozen package universe at `e4c8e77` is 16 production packages
  plus test-only `internal/specfirst`.
- At `e4c8e77`, store completed exact calibrated partitions totaling 133 candidates: 133 killed,
  0 invalid, 0 survived. Twin completed 31 candidates: 31 killed, 0 invalid, 0 survived. Standalone
  tagged store and twin suites passed. The disposable PostgreSQL instance was stopped and removed.
- Evidence is committed in `docs/verification/p6-task8-store-twin-mutation-e4c8e77.md`. C3 and Task 9
  remain open because the final cross-package packet and current-code non-author verdict have not
  been produced; author-side mutation evidence cannot substitute for that verdict.

LESSON: A mutation run that shares a database across processes must reset the database between
partitions, and deliberate rollback mutants can leave schema state altered after process exit. A
fresh disposable database is required before later partitions and before baseline evidence.
