# Journal — 2026-09-16

## P6 Task 8 — frozen-commit rerun checkpoint and EOD

- Inspected the repository’s committed state rather than relying on session recollection. The
  day’s implementation freeze remains `e4c8e77`; today’s committed checkpoint is `a199229`, which
  records complete calibrated reruns for 12 packages. Store and twin have separate complete
  frozen-commit evidence. The package-universe test passes; `internal/specfirst` remains test-only.
- The complete rerun evidence covers core, checks, Git, gitcmd, GitHub, intent, intent records,
  intent specdir, reviews, sessions, gates, and migration. A first untagged gates probe reported
  38 survivors because it omitted integration proving tests; it is diagnostic only. The correctly
  tagged gates run completed 109/109 killed, 0 invalid, 0 survived.
- Frozen-commit CLI and projections reruns were attempted, then interrupted before complete
  summaries because database-backed runs progressed too slowly. Their partial output is not
  acceptance evidence. No product code was changed. The final 16-package packet and current-code
  non-author verdict remain open.
- Repository-scoped session artifact locations (`notes/tmp`, `.codex`, `.agents`, and the expected
  user session directory) contained no session files. No PostgreSQL or mutation workers and no
  temporary P6 database directories remained after inspection.

LESSON: Before launching an expensive database-backed mutation sweep, run a bounded calibrated
pilot and measure throughput; choose a capped concurrency based on that measurement, with a
separate disposable database per concurrent partition. Do not interpret or preserve partial output
as a result; only complete calibrated summaries count.

## End-of-day verification and durability

- The first bare `make check` completed the hook suite, then stopped at `commit-cadence`: HEAD was
  older than 90 minutes while the only dirty paths were the two pre-existing untracked user files
  listed in `notes/state.md`. This was a real gate failure, not an expected negative control.
- After committing this documentation checkpoint, a second bare `make check` passed cadence and
  all repository checks but could not find `golangci-lint` in the elevated shell's PATH. A rerun
  with `PATH=/home/thamm/go/bin:$PATH` passed hooks, freshness, cleanroom, index/invariant and
  package checks, and reached kernel lint after Go build/tests; golangci-lint v2.13.2 then panicked
  because it was built with Go 1.26 and encountered Go 1.27 source. This is an environment/tool
  compatibility blocker, not a reported source lint violation.
- `scripts/index-check.sh` and `scripts/invariant-lint.sh` passed directly. The hook suite printed
  `index stale; run make index` from its intentional stale-index negative-control test and
  continued; this was expected diagnostic output, not an observed live index failure.
- Remote inspection found `origin` configured, `origin/main` is an ancestor of local `main`, and
  GitHub CLI authentication is active for `kamisrini` with `repo` scope. The user’s EOD request
  conditionally authorized publishing the completed local history. `git push origin main` succeeded
  from `1d1112d` through `8377716`; a fresh `git ls-remote` confirmed remote main at
  `83777166b8fec1beccd06fa008ac121615ba6e03`.
- `make backup` created `/home/thamm/Backups/proofbound-20260916T232408Z.bundle` (SHA-256
  `c12d773ecb3bf69c6d61439dd3fa5b5fed3c7f28446f304de366ac41276e49e9`). `git bundle verify`
  succeeded, and a bare clone's `refs/heads/main` exactly matched `83777166b8fec1beccd06fa008ac121615ba6e03`.
- This closeout status entry is documentation-only. The final state-only checkpoint is included in
  a fresh verified bundle and pushed before handoff; no implementation was modified.

LESSON: Long-lived, pre-existing untracked artifacts can trip commit-cadence even when no tracked
work is pending. Keep their ownership explicit and untouched; a coherent, factual state/journal
checkpoint commit refreshes the cadence without absorbing those files.
