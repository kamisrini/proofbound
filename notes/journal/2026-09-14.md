# Journal — 2026-09-14

## P6 Task 5 — requirement-review completion

- Resumed from `notes/state.md`; branch `main` was clean except for the pre-existing untracked
  `docs/plans/P5-intent-provenance-plan.md`.
- Read the P6 Task 5 contract and existing requirement-review SPEC. The repository has exactly two
  configured production providers, `records` and `specdir`; only one active committed requirement
  revision exists, with active obligations `O-1` and `O-2`.
- Added `docs/plans/p6-task5-requirement-review-SPEC.md`, the fail-closed checker
  `scripts/p6-task5-review.sh`, its hostile fixture test, and generated closure packet
  `docs/verification/p6-requirement-review.md`. The packet binds the exact requirement digest and
  the exact non-author review, and reports `VERIFIABLE` for both active obligations.
- Focused `go test ./internal/connector/reviews ./internal/projections -run 'RequirementReview|Intent'
  -count=1` passed with host Go execution. The sandboxed Snap Go attempt was a capability error,
  not a test failure. The Task 5 hostile checker test passed.

LESSON: When testing a newly added shell mechanism in a cloned fixture, ensure the mechanism itself
is committed or explicitly copied into the fixture; an untracked script is absent from a Git clone
and can make an expected-failure test pass for the wrong reason.

## Verification follow-up

- The first bare `make check` after the Task 5 commit reached the repository checks but failed at
  `kernel-check` because the sandboxed Snap Go toolchain lacks `cap_dac_override`; the same run also
  exposed a real stale generated Task 2 packet.
- Regenerated `docs/verification/p6-connector-reality.md`; its code binding now resolves to the
  current connector implementation commit `d6abc5b3c777fb08f9ddf840bc8444db7c9f5f3d`, and its
  checker passes.
- The Task 5 hostile checker remained green. Host bare `PATH=/home/thamm/go/bin:$PATH make check`
  passed with exit 0. The run's `index stale; run make index` output was the expected negative
  control; the final gate completed with `0 issues`.
- Task 5 is complete. Exact next action is Task 6: spec-first enumeration and provenance-bound
  evaluation of all nine P5 measurements and seven falsifiers.

## P6 Task 6 — measurements and falsifiers

- Added the exact-result SPEC, 16-row result registry, fail-closed checker, hostile fixture tests,
  and generated packet `docs/verification/p6-measurements-falsifiers.md`.
- The packet reports exactly 9 measurements and 7 falsifiers. It records four measured values and
  five not-recoverable measurements; no missing historical timing or review datum was fabricated.
- All 16 C7 census rows now cite the generated packet. Task 6 hostile tests and the existing Task 5
  hostile tests pass.

## P6 Task 7 — conditional snapshot provider

- Verified the founder-ratified decision and semantic VD: Task 7 is skipped, no export feed is
  designated, and the no-width doctrine remains intact. The pinned provider contract remains for
  separately authorized P7+ work.
- `scripts/p6-census.sh --check` and `scripts/p6-artifact-integrity.sh --check` passed after the
  Task 6 commit. Host bare `PATH=/home/thamm/go/bin:$PATH make check` passed with exit 0 and
  `0 issues`; expected negative-control output was not a failure.
- Exact next action is Task 8: spec-first current-package acceptance and mutation sweep binding.

## P6 Task 8 — package universe

- Host `go list ./internal/...` returned 17 packages. Sixteen contain non-test production Go files;
  `internal/specfirst` contains only `SPEC.md` and `_test.go` and is now explicitly classified in
  census row C3-017 as not in the production package set.
- Added the Task 8 package-acceptance SPEC and package-universe invariant test. The test passed.
- No mutation result or non-author final package verdict has been claimed. Those remain required
  before C3 closure and before Task 9.

## P6 Task 8 — internal/core mutation remediation

- The initial calibrated `internal/core` sweep reported 62 candidates, 39 killed, 0 invalid, and
  23 survivors. Added invariant tests for default clock initialization, nested malformed JSON and
  decoder errors, nested unsafe numbers, invalid event fields, and source/native-id-sensitive
  idempotency semantics.
- A second sweep reported 62 candidates, 40 killed, and 22 survivors. Splitting compound guards in
  `Canonicalize` and `inspectValue` removed two semantics-preserving mutation sites; a focused
  nested duplicate-key propagation assertion removed the remaining two error-swallowing survivors.
- Final calibrated result: 58 candidates, 58 killed, 0 invalid, 0 survived. Host
  `go test ./internal/core -count=1` passed.

LESSON: Mutation survivors that preserve a generic error result can evade weak negative tests;
assert the error's semantic identity or distinguishing detail when the contract requires
propagation, and simplify compound guards when a mutation is semantically equivalent.

## P6 Task 8 — internal/cli mutation audit

- The first CLI run was interrupted after candidate 81 while investigating dispatch survivors; it
  was not accepted as a result. A semantics-preserving switch refactor removed the dispatch mutation
  sites and focused `go test ./internal/cli -count=1` passed.
- The restarted run was then stopped after it exposed numerous survivors in unexercised
  `runCommand` and verification/helper error paths. No final candidate summary was produced, so no
  CLI package closure or acceptance claim is recorded. The source refactor remains uncommitted.

LESSON: A mutation run that is stopped before its summary is diagnostic only. Package acceptance
requires a complete rerun after remediation, and error-path coverage must be derived from the
observable failure contracts rather than inferred from happy-path command parsing tests.

- Added a focused `verifyStep` trace-output assertion and a narrow `eventReader` seam with empty
  and non-empty ledger invariants for `latestLedgerCheckRunID`; focused CLI tests remain green.
- The next fail-fast probe killed the trace, witness, invalid-repository, and ledger-empty-result
  survivors. It now reaches the `intentrecords.New` guard in `syncGit`, whose inputs are always
  non-nil and non-zero by construction; this is not yet declared or accepted because the complete
  package sweep and independent review remain outstanding.

- Added and passed direct CLI invariants for `verifyStep` cancellation/error/trace behavior,
  `latestLedgerCheckRunID` empty and non-empty results through a narrow event-reader seam, and
  `syncGit` invalid-repository error identity. The changes are behavior-preserving and are being
  committed as a coherent remediation slice; mutation acceptance remains open.

## P6 Task 8 — CLI mutation continuation

- Started a disposable embedded PostgreSQL service using the repository's cached PostgreSQL
  binaries because Docker and system PostgreSQL were unavailable. The tagged CLI integration suite
  passed against it; the temporary probe was stopped and removed before the durability commit.
- The complete pre-commit calibrated CLI sweep ran all 234 candidates and reported 233 killed,
  0 invalid, and one survivor at `cli.go:347#233`. The survivor was a real fixture gap: the
  committed-intent reader's Markdown filter was not distinguished because the fixture had only
  Markdown files.
- Added a committed `notes.txt` fixture artifact and retained the assertion that only Markdown
  artifacts are returned. The focused CLI suite passed, and targeted candidate `233` was killed
  with calibration `neutral=survived invalid=invalid lethal=killed`.
- No final CLI package acceptance is claimed yet. The remediation still needs a durability commit
  followed by a complete calibrated tagged sweep against that committed implementation hash.

LESSON: A filter mutation can survive a happy-path fixture when the excluded class is absent. For
file-type or allowlist invariants, every proving fixture should contain at least one included item,
one excluded item, and the assertion should count the resulting set.

- Committed the CLI remediation as `3f115ea`, then reran the complete calibrated integration-tagged
  sweep against that frozen implementation. Final result: 234 candidates, 234 killed, 0 invalid,
  0 survived. The exact package tree and command are recorded in
  `docs/verification/p6-task8-cli-mutation-3f115ea.md`.
- Stopped and removed the temporary PostgreSQL probe after the acceptance run. The next package in
  the frozen sequence is `internal/connector/checks`; no non-author final package verdict exists yet.
- `internal/connector/checks` then completed its calibrated sweep against frozen implementation
  `3f115ea`: 36 candidates, 36 killed, 0 invalid, 0 survived. Its exact tree binding is recorded
  in `docs/verification/p6-task8-checks-mutation-3f115ea.md`.
- `internal/connector/git` then completed its calibrated sweep against frozen implementation
  `3f115ea`: 34 candidates, 34 killed, 0 invalid, 0 survived. Its exact tree binding is recorded
  in `docs/verification/p6-task8-git-mutation-3f115ea.md`.

## P6 Task 8 — gitcmd remediation and frozen revalidation

- The complete `internal/connector/git/gitcmd` sweep at the prior freeze reported 86 candidates,
  82 killed, 0 invalid, and four survivors. The survivors covered `Reachable` shallow/broken-ref
  preconditions, the `filepath.Rel` failure branch, and the intent-trailer separator guard.
- Added the missing Reachable fixtures, a narrow `filepath.Rel` seam with an injected-error test,
  and a no-separator trailer fixture. Focused gitcmd tests and all four targeted mutations passed.
- Committed the remediation as `a5ef803`. Complete revalidation at that frozen hash is green for
  gitcmd (86/86), CLI (234/234), checks (36/36), and Git (34/34); all have zero invalid and
  surviving mutants. The aggregate exact-tree packet is
  `docs/verification/p6-task8-package-results-a5ef803.md`.
- The temporary embedded PostgreSQL probe was stopped and removed. The next package is
  `internal/connector/github`; no current-code non-author final verdict exists yet.

LESSON: When a code change advances the package freeze, previously green mutation results must be
rerun or they cannot share one final acceptance commit. Parent-package tree objects also change
when a nested package changes, even if the parent source is untouched.
